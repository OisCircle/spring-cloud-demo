spring:
  application:
    name: eureka-client
  cloud:
    config:
      uri: http://localhost:1005/
      profile: dev
      label: master
server:
  port: 1006
management:
  endpoints:
    web:
      exposure:
        include: refresh