spring:
  application:
    name: api-gateway
    cloud:
      config:
        uri: http://localhost:1005/
        profile: dev
        label: master
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1000/eureka/
server:
  port: 1007